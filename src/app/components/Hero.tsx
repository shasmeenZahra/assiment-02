// Hero.js
import styles from './Hero.module.css';

const Hero = () => (
    <section className={styles.hero}>
        <h2 className={styles.heading}>Welcome to My Portfolio</h2>
        <p className={styles.subheading}>Explore my work and get to know me!</p>
    </section>
);

export default Hero;
